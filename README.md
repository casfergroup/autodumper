# Autodumper
Autodumper is a docker image that backups databases and sends the data to a remote host using S3. It can be useful for performing hot backups and storing them on S3.

## Supported Databases
- MySQL
- MariaDB